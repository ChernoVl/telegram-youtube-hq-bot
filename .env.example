# Copy to .env (for local dev only). On Railway, set these in Variables.
TELEGRAM_BOT_TOKEN=123456:ABC-your-token-here
# Optional: choose format (default set in code if unset)
# YTDLP_FORMAT=bestvideo*+bestaudio/best
